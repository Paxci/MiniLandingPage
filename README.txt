Ya que se utilizan fuentes de imagenes directamente de fuentes protegidas, al abrir el archivo html
directamente, este no ejecuta el archivo javascript.

Para corregir este error se debe de correr el proyecyo sobre un server tipo xamp o con la extencion liveServer
de visual studio code.